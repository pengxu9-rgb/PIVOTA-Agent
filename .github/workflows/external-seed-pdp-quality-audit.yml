name: External Seed PDP Quality Audit

on:
  workflow_dispatch:
    inputs:
      market:
        description: "Market code"
        required: true
        default: "US"
        type: string
      domain:
        description: "Optional exact seed domain"
        required: false
        default: ""
        type: string
      brand:
        description: "Optional exact seed brand"
        required: false
        default: ""
        type: string
      external_product_id:
        description: "Optional single external_product_id"
        required: false
        default: ""
        type: string
      limit:
        description: "Rows to scan"
        required: true
        default: "100"
        type: string
      offset:
        description: "Row offset"
        required: true
        default: "0"
        type: string
      skip_similar:
        description: "Skip similar probe"
        required: true
        default: false
        type: boolean
      concurrency:
        description: "Live PDP probe concurrency"
        required: true
        default: "4"
        type: string

jobs:
  pdp-quality-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment:
      name: Pivota Agent / production
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      PIVOTA_BACKEND_AGENT_API_KEY: ${{ secrets.PIVOTA_BACKEND_AGENT_API_KEY }}
      SHOP_GATEWAY_AGENT_API_KEY: ${{ secrets.SHOP_GATEWAY_AGENT_API_KEY }}
      PIVOTA_AGENT_API_KEY: ${{ secrets.PIVOTA_AGENT_API_KEY }}
      AGENT_API_KEY: ${{ secrets.AGENT_API_KEY }}
      CATALOG_INTELLIGENCE_BASE_URL: ${{ secrets.CATALOG_INTELLIGENCE_BASE_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci --ignore-scripts

      - name: Run PDP quality audit
        shell: bash
        env:
          IN_MARKET: ${{ inputs.market }}
          IN_LIMIT: ${{ inputs.limit }}
          IN_OFFSET: ${{ inputs.offset }}
          IN_CONCURRENCY: ${{ inputs.concurrency }}
          IN_DOMAIN: ${{ inputs.domain }}
          IN_BRAND: ${{ inputs.brand }}
          IN_EXTERNAL_PRODUCT_ID: ${{ inputs.external_product_id }}
        run: |
          set -euo pipefail
          if [[ -z "${DATABASE_URL:-}" ]]; then
            echo "DATABASE_URL secret is required" >&2
            exit 1
          fi

          out_dir="reports/workflow_ops/external-seed-pdp-quality-audit/${{ github.run_id }}"
          mkdir -p "${out_dir}"

          args=(
            --market "${IN_MARKET}"
            --limit "${IN_LIMIT}"
            --offset "${IN_OFFSET}"
            --concurrency "${IN_CONCURRENCY}"
            --out "${out_dir}/report.json"
          )
          if [[ -n "${IN_DOMAIN}" ]]; then
            args+=(--domain "${IN_DOMAIN}")
          fi
          if [[ -n "${IN_BRAND}" ]]; then
            args+=(--brand "${IN_BRAND}")
          fi
          if [[ -n "${IN_EXTERNAL_PRODUCT_ID}" ]]; then
            args+=(--external-product-id "${IN_EXTERNAL_PRODUCT_ID}")
          fi
          if [[ "${{ inputs.skip_similar }}" == "true" ]]; then
            args+=(--skip-similar)
          fi
          node scripts/audit-external-seed-live-pdp-modules.cjs "${args[@]}" \
            | tee "${out_dir}/summary.json"

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: external-seed-pdp-quality-audit-${{ github.run_id }}
          path: reports/workflow_ops/external-seed-pdp-quality-audit/${{ github.run_id }}
          retention-days: 7
