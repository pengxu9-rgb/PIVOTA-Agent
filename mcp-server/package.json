{
  "name": "pivota-mcp-server",
  "version": "0.1.0",
  "description": "Pivota MCP commerce surface. Production = gateway remote /mcp (OAuth, commerceToolSurface tools). This package's `start` is the LEGACY local stdio adapter (pivota_* tools, reads-only over stdio) — see README.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0"
  }
}
